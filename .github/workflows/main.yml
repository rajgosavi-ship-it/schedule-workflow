name: Daily Scheduled Workflow

on:
  schedule:
    - cron: '35 16 * * *'

jobs:
  run-script:
    runs-on: ubuntu-latest
    
    steps:
      - name: Print current time
        run: echo "Running the scheduled workflow at $(date)"
