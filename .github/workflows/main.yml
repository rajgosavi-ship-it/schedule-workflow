name: Run Manually or Scheduled

on:
  workflow_dispatch:  # 👈 allows manual trigger
  schedule:
    - cron: '*/5 * * * *'  # (optional) runs every 5 minutes

jobs:
  run-task:
    runs-on: ubuntu-latest
    steps:
      - name: Run your job
        run: echo "Running workflow at $(date -u)"

